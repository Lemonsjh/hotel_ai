## 1. 读取层渠道闸门

- [x] 1.1 为 `hotels.config_json.channels` 过滤 OTA 表 key 编写失败测试。
- [x] 1.2 在 MySQL V4 OTA 读取层接入启用渠道解析和 metadata 风险标记。

## 2. 验收

- [x] 2.1 运行 OpenSpec strict validate。
- [x] 2.2 运行数据库适配器相关单元测试。
