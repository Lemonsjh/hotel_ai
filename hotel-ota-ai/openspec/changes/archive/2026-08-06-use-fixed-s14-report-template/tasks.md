# Tasks

- [x] 更新 S14 报告契约测试，先验证当前随机文件名和简化 HTML 会失败。
- [x] 将本地报告文件名改为 `ota_diagnosis_report.html`，`report_id` 改为 `ota_diagnosis_report`。
- [x] 按示例模板风格重写 S14 HTML 结构，保留现有计算结果，不注入演示事实。
- [x] 保留遗留 `s14-*.html` 清理逻辑，并确认不删除无关 HTML。
- [x] 运行 OpenSpec 和相关 unittest 验证。
