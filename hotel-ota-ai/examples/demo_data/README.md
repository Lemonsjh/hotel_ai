# Demo Data

This directory contains non-production fixtures for full-chain demos.

Rules:

- Every fixture is `data_source_type=demo_data` and `freshness_status=demo_data`.
- Demo data can drive previews, dry-runs, report previews, and message previews.
- Demo data cannot create formal approvals or live execution.
- Demo results must not be described as today's real business facts.
