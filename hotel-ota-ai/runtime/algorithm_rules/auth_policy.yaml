{
  "schema_version": "1.0",
  "owner_skill": "S1",
  "status": "implemented",
  "placement": "runtime/algorithm_rules",
  "node_id": "N003",
  "agent_id": "A0",
  "phase": "P0",
  "source_refs": {
    "node_registry": "architecture/node_registry.json",
    "edge_registry": "architecture/edge_registry.json",
    "scenario_registry": "architecture/scenario_chain_registry.json",
    "field_registry": "contracts/v27/field_registry.yaml",
    "node_io_contract": "contracts/v27/node_io_contract.yaml",
    "skill_alignment": "skills/hotel-ota/s01-control-config/references/v27_alignment.json"
  },
  "demo_safety": {
    "data_source_type": "demo_data",
    "freshness_status": "demo_data",
    "business_status": "demo_or_historical",
    "today_label_allowed": false,
    "approval_data_allowed": false,
    "live_allowed": false
  },
  "data_gate": {
    "hard_required": [
      "auth_source",
      "open_id_or_user_id",
      "action"
    ],
    "soft_required": [],
    "missing_policy": "block_business_conclusion_or_live; allow demo preview only when demo safety flags are present"
  },
  "algorithm": {
    "objective": "Build verified auth_context and permission gate before any business skill runs.",
    "ordered_steps": [
      {
        "step_id": "s1_01",
        "action": "Normalize Feishu ou_* as open_id unless an explicit user_id is supplied."
      },
      {
        "step_id": "s1_02",
        "action": "Resolve role only from /etc/hotel-ota-ai/feishu-role-map.json or verified runtime auth_context."
      },
      {
        "step_id": "s1_03",
        "action": "Map action to required permission and deny missing_identity/guest for business skills."
      },
      {
        "step_id": "s1_04",
        "action": "Emit auth_context and permission_gate; never infer admin/owner from chat memory."
      }
    ],
    "thresholds": {
      "guest_business_skill_allowed": false,
      "frontdesk_revenue_allowed": false,
      "operator_live_allowed": false
    },
    "degradation": [
      "When hard-required fields are missing, return missing_fields and confidence=low.",
      "When input is demo/sample/stale, return preview_only or dry_run and block formal approval/live.",
      "When source capability is read-only/manual, produce recommendation or task only."
    ],
    "output_contract": [
      "auth_context",
      "required_permission",
      "allowed",
      "reason"
    ]
  },
  "forbidden_actions": [
    "treat_demo_data_as_real_today_data",
    "create_formal_approval_from_demo_or_stale_data",
    "bypass_data_gate_or_approval_guard"
  ],
  "demo_behavior_cases": [
    {
      "case_id": "s1_demo_preview",
      "fixture": "examples/demo_data/nodes/N003.json",
      "expected": {
        "data_source_type": "demo_data",
        "approval_data_allowed": false,
        "live_allowed": false,
        "status_in": [
          "preview_only",
          "dry_run",
          "simulation_only",
          "ok"
        ]
      }
    },
    {
      "case_id": "s1_missing_hard_required",
      "input": {
        "hotel_id": "puyue-demo"
      },
      "expected": {
        "allowed": false,
        "reason": "missing_hard_required_fields"
      }
    }
  ],
  "executed_by_runtime": [
    "runtime.safety.auth.permission_gate"
  ],
  "runtime_execution_status": "executed"
}
