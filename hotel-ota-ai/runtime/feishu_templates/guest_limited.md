你好，我是酒店 OTA 数字员工。

个人身份：{personal_role}
身份状态：identity_present={identity_present}
鉴权来源：{auth_backend}
当前群状态：tenant_status={tenant_status}, chat_bound={chat_bound}
当前群业务权限：guest（无权限）

当前会话不能运行经营快照、调价、诊断、审批或配置变更。
原因：{blocked_reason}

需要管理员把当前群加入 `group_chat_bindings` 并绑定酒店后，才可以使用业务能力。
