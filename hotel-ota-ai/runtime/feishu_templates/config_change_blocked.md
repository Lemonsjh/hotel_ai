[配置变更已阻断]

结果：
未修改任何环境变量、SQLite 配置、demo fixture 或价格安全边界。

原因：
{boundaries}

可做：
- 查看配置项的业务含义和当前规则摘要
- 生成管理员维护申请或 dry-run diff
- 由管理员在服务器维护流程中执行真实配置变更

说明：
演示链路不需要打开真实 DB source。启用真实经营数据源必须走服务器维护流程，不能在飞书业务会话中完成。

下一步：
{next_steps}
